{
  "name": "pg-toolbox",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit --incremental false"
  },
  "dependencies": {
    "pg": "^8.2.1"
  },
  "devDependencies": {
    "@types/pg": "^7.14.3",
    "typescript": "^3.9.5"
  }
}
