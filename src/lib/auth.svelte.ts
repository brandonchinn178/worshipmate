export const session = $state(null)

export const loadAuth = () => {
  // TODO: add supabase auth
  // supabase.auth.getSession().then(({ data }) => {
  //   session = data.session
  // })
  // supabase.auth.onAuthStateChange((_event, _session) => {
  //   session = _session
  // })
}
